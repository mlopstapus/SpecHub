apiVersion: v1
kind: Secret
metadata:
  name: {{ include "spechub.fullname" . }}
  labels:
    {{- include "spechub.labels" . | nindent 4 }}
type: Opaque
data:
  database-url: {{ include "spechub.databaseUrl" . | b64enc | quote }}
  auth-token: {{ .Values.backend.authToken | b64enc | quote }}
  postgresql-password: {{ .Values.postgresql.password | b64enc | quote }}
