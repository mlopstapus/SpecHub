apiVersion: v1
kind: Service
metadata:
  name: {{ include "spechub.backend.fullname" . }}
  labels:
    {{- include "spechub.labels" . | nindent 4 }}
    {{- include "spechub.backend.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spechub.backend.selectorLabels" . | nindent 4 }}
