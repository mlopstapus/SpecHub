{{- if .Values.backend.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "skillcanon.backend.fullname" . }}
  labels:
    {{- include "skillcanon.labels" . | nindent 4 }}
    {{- include "skillcanon.backend.selectorLabels" . | nindent 4 }}
  annotations:
    router.openshift.io/disable-http2: "true"
spec:
  {{- if .Values.backend.route.host }}
  host: {{ .Values.backend.route.host | quote }}
  {{- end }}
  to:
    kind: Service
    name: {{ include "skillcanon.backend.fullname" . }}
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
{{- end }}
