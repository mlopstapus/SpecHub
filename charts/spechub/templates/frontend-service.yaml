apiVersion: v1
kind: Service
metadata:
  name: {{ include "spechub.frontend.fullname" . }}
  labels:
    {{- include "spechub.labels" . | nindent 4 }}
    {{- include "spechub.frontend.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spechub.frontend.selectorLabels" . | nindent 4 }}
