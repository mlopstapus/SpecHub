# Phase 1 Data Model: Next.js App Scaffolding

**N/A for this feature.**

This feature scaffolds project structure only — no database schema, no domain entities, and no Drizzle models are introduced here. `shared/db` is created as a placeholder folder (see `research.md`) with no live connection or schema.

The first real data model lands with the bounded-context epics that follow this one (starting with `002-identity-access`), applying the conventions already decided in `context/database-conventions.md` (standard columns, RLS pattern, migration workflow).
